#include <iostream>
#include <string>

int main() 
{
    // code should output numbers 1 through 10 on a separate line

    int i = 0;
    while (i < 10) {
        i += 1;
        std::cout << i << std::endl;
    }
}